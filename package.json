{
  "name": "agent-listener-audio",
  "version": "1.0.0",
  "description": "Aplicación para grabar audio del micrófono y guardarlo en un archivo",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install-whisper": "git clone https://github.com/ggerganov/whisper.cpp.git && cd whisper.cpp && git checkout v1.5.4 && rm -rf .git && make && bash ./models/download-ggml-model.sh base"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ffmpeg-static": "^5.3.0",
    "multer": "^1.4.5-lts.1",
    "qrcode-terminal": "^0.12.0",
    "selfsigned": "^2.4.1"
  }
}